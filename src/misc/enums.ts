export enum Path {
  resume = '/resume',
  hobby = '/hobby',
  project = '/project',
  home = '/',
}

export enum StyleTheme {
  dark = 'theme-dark',
  light = 'theme-light',
  none = 'theme-none',
}

export enum LocalStorageKeys {
  theme = 'theme',
}
