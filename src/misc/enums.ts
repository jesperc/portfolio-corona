export enum Path {
  resume = '/resume',
  home = '/',
}

export enum StyleTheme {
  dark = 'theme-dark',
  light = 'theme-light',
  none = 'theme-none',
}

export enum LocalStorageKeys {
  theme = 'theme',
}
